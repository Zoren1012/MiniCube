plugins {
    id 'net.fabricmc.fabric-loom' version "${loom_version}"
}

version = project.mod_version
group = project.maven_group

base {
    archivesName = project.archives_base_name
}

dependencies {
    minecraft "com.mojang:minecraft:${project.minecraft_version}"
    // Depuis Loom 1.17, les mappings officiels de Mojang servent par defaut et les mods
    // se declarent comme des dependances ordinaires : le plugin les repere et les
    // remappe lui-meme. Le code utilise donc les vrais noms des classes du jeu.
    implementation "net.fabricmc:fabric-loader:${project.loader_version}"
    implementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_api_version}"
}

processResources {
    inputs.property "version", project.version
    filesMatching("fabric.mod.json") {
        expand "version": project.version
    }
}

tasks.withType(JavaCompile).configureEach {
    // Minecraft 26.2 tourne sur Java 25 : viser plus bas ne compilerait pas contre le
    // jeu, viser plus haut produirait un mod qu'il refuserait de charger.
    it.options.release = 25
    it.options.encoding = 'UTF-8'
}

java {
    sourceCompatibility = JavaVersion.VERSION_25
    targetCompatibility = JavaVersion.VERSION_25
}
